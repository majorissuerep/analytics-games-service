{
  "name": "@analytics-games/orbit-pinball",
  "version": "1.0.0",
  "private": true,
  "description": "Manifest workspace for the vendored upstream browser pinball game",
  "scripts": { "check": "tsc --noEmit -p tsconfig.json" },
  "peerDependencies": { "react": ">=19" }
}
